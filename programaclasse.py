#programa que te funcio SUMA(llista), mostri cada element x10 pero 
# quan acabi funcio no modifica la llista i que retorni llista amb nous valors
"""
def suma(l):
    ls=[]
    for i in range(len(l)):
        ls.append(i)
    return ls

#PROGRAMA PRINCIPAL
l = [1, 2, 3, 4, 5]
print(l)
s=suma(l)
print("La llista original es {} i la modificada es {}".format (l,s))
"""

#Programa + funcio sllista(llista), retorni a suma de tots els elements

def sllista(llista):
    ls=[]
    for e in range(len(llista)):
        ls.append(e)
    return llista

#programa principal
